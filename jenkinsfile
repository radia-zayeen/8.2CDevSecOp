pipeline {
  agent any
  stages {
    stage('Email Test') {
      steps {
        emailext(
          subject: "Test Email from emailext",
          body: "This is a test email sent using the Email Extension Plugin.",
          to: 'radia.zayeen@gmail.com'
        )
      }
    }
  }
}
